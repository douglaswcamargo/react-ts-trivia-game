export interface CustomTheme {
  colors: {
    primary: string
    success: string
    error: string
    text: string
    control: string
    icon: string
    bodyBackground: string
    sectionBackground: string
  }
  fonts: {
    primary: string
  }
}
